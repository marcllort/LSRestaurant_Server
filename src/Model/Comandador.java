package Model;

import java.util.ArrayList;

/*public class Comandador {

    private ArrayList<Comanda> comandes;


    public Comandador() {
        comandes = new ArrayList<>();                                                           //Inicialitzo el arraylist que conte les comandes
    }

    public synchronized void addComanda(Comanda c) {
        comandes.add(c);                                                                        //Metode per afegir comandes
    }

    public synchronized Comanda getComandaAt(int index) {
        return comandes.get(index);                                                             //Metode per rebre comanda en una posicio determinada
    }

    public synchronized String getAllComandes() {                                               //Contrueix un string a partir de totes les comandes
        StringBuilder stringBuilder = new StringBuilder();

        for (Comanda m : comandes) {
            stringBuilder.append(m).append(System.lineSeparator());
        }

        return stringBuilder.toString();
    }

    public synchronized void removeComandaAt(int index) throws IndexOutOfBoundsException {      //Metode per borrar comanda en una posicio determinada
        comandes.remove(index);
    }

    public synchronized int sizeComanda() {
        return comandes.size();                                                                 //Retorna tamany del arraylist
    }

}*/
